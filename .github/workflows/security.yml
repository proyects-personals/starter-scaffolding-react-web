name: Security & Dependencies

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:
jobs:
  security:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Audit NPM packages
        run: |
          mkdir -p reports
          # Ejecuta el comando de auditoría y guarda el reporte en formato JSON
          npm audit --json > reports/npm-audit.json
          cat reports/npm-audit.json  # Muestra el reporte en los logs

      - name: Check outdated packages
        run: |
          npm outdated || true  # Esto no falla si no hay dependencias desactualizadas

      - name: Upload security report
        uses: actions/upload-artifact@v4
        with:
          name: security-report
          path: reports/npm-audit.json 

  fork-compatibility:
    runs-on: ubuntu-latest
    needs: security
    steps:
      - name: Check if it is a fork
        run: |
          if [ "${{ github.event.pull_request.head.repo.fork }}" == "true" ]; then
            echo "✅ This is a fork, proceeding with limited actions."
          else
            echo "❌ This is not a fork, skipping additional steps."
          fi